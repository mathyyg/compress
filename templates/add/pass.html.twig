{% extends 'base.html.twig' %}

{% block title %}Ajoutée une pages!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="form">
    {% if ressource %}
    <form action="{{ path('app_view', { 'url': ressource.url }) }}" method="GET">
        <div>
            <label for="pass">Mots de passe</label>
            <input type="text" id="pass" name="pass" placeholder="mots de passe" required>
            <form>
        </div>

        <div>
            <input type="submit" value="allez" >
        </div>
    </form>
    {% endif %}
</div>
{% endblock %}
